<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newmandal">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".MyPrefrence"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.IntrestListActivity" />
        <activity android:name=".Activity.EditmemberActivity" />
        <activity android:name=".Activity.EditIntrestActivity" />
        <activity android:name=".Activity.AddInstollmentActivity" />
        <activity android:name=".Activity.ShowInstolmentActivity" />
        <activity android:name=".Activity.AddcreditActivity" />
        <activity android:name=".Activity.MemberDetailActivity" />
        <activity android:name=".Activity.AddMemberActivity" />
        <activity android:name=".Activity.DesbordActivity" />
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>